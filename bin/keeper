#!/usr/bin/env bash
: ${KEEPERRC:=~/.keeperrc}
[[ $KEEPERRC ]] && . $KEEPERRC
[[ $KEEPER_CONFIG ]] && . $KEEPER_CONFIG
which keeper &>/dev/null || PATH+=:$(dirname $0)
exec keeper-${1---help} "${@:2}"
